<div class="input-button">
  <bx-btn icon-layout type="submit"
    class="button mt-5"
    onclick="{{attributes.onclick}}"
    name="{{attributes.name}}"
    type="{{attributes.type}}"
    value="{{attributes.value}}"
    {{#if attributes.disabled}}disabled{{/if}}
  >
    {{getNodeLabel .}}
  </bx-btn>
  {{#if messages}}
    <span class="button-helper">
      {{> messages }}
    </span>
  {{/if}}
</div>

